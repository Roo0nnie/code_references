
	
		Binary Tree is defined as a Tree data structure with at most 2 children. Since each element in a binary tree can have only 2 children,
	we typically name them the left and right child.

		
		BINARY TREE REPRESENTATION								BINARY OPERATION ON BINARY TREE
										
		A Binary tree is represented by a pointer to the topmost 			* Inserting an element.
	node of the tree. If the tree is empty, then the value of the 				* Removing an element.
	root is NULL. 										* Searching for an element.
												* Traversing an element.
		Binary Tree node contains the following parts:
				
		1. Data											AUXILIARY OPERATION ON BINARY TREE
		2. Pointer to left child														 
		3. Pointer to right child							* Finding the height of the tree			
												* Find the level of the tree
												* Finding the size of the entire tree.